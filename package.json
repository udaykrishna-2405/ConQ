{
  "name": "conq",
  "version": "1.0.0",
  "private": true,
  "description": "ConQ — AI Creator Intelligence Platform",
  "scripts": {
    "dev": "concurrently --prefix-colors cyan,magenta --names BACKEND,FRONTEND \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run start --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "test": "npm run test --prefix backend",
    "test:security": "npm run test:security --prefix backend",
    "deploy": "npm run deploy --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.9.3"
  }
}
